name : Mediclinic CI/CD Pipeline

on: [push]

jobs:
    build:
        runs-on: ubuntu-latest
    
        steps:
        - name: Checkout
          uses: actions/checkout@v4
        - name: Run Cypress test
          uses: cypress-io/github-action@v6
          with:
            node-version: 20
